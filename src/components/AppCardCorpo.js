import { Outlet } from 'react-router-dom';

const AppCardCorpo = () => {
    return (
        <div className="aula-body">
            <Outlet />
        </div>
    );
};

export default AppCardCorpo;
