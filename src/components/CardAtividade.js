import CardMenuAtividade from "./CardMenuAtividade";

const CardAtividade = () => (
        <div className="content">
            <CardMenuAtividade />
        </div>
    );

export default CardAtividade;