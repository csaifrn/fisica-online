import { AppNavBar, AppCard } from '../components';

const PageLayout = () => {
    return (
        <>
            <AppNavBar />
            <AppCard />
        </>
    );
};

export default PageLayout;
